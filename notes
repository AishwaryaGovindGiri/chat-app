//------------------ commands at start in server folder -------------------------------
// npm init -y 
// npm install bcryptjs
// =>this is to encrypt the passwords then store in mongodb
// npm install cloudinary
// =>store chat img and user profile image 
// npm install cors
// -> it will allow backend to connect with any frontend url
// npm install dotenv
// => we can use env varibles in backend server
// npm install express
// => using this we will create backend server
// npm install jsonwebtoken
// =>using genrate token and authinticate users using token 
// npm install mongoose
// =>connect with mongodb 
// npm install socket.io
// =>real time chat 


// --------changes before backedn code start ----
// "type":"commonjs",
// change this to module in packagejs 
// so we cna use import and export instead of {{require && module.export}}
// it tells all .js file can be imported using import no need of require 
// Tells Node:
// "Use modern JavaScript module system."
// "type":"module",


//--------
//"test": "echo \"Error: no test specified\" && exit 1"
this line changed to this -----> bcs we need nodemon in "package.json"
"server": "nodemon server.js",
this automatically restart server when change happen in code , (no need to do manullay)

//npm run server
start the server


//in client 
//install axios  for api call
//install react-hot-toast 
//install socket io for real time communication 